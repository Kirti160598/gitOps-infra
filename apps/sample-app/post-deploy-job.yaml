apiVersion: batch/v1
kind: Job
metadata:
  name: post-deploy-check
spec:
  template:
    spec:
      containers:
        - name: healthcheck
          image: curlimages/curl
          command: ["sh", "-c", "curl -sf http://sample-app.default.svc.cluster.local && echo App is up!"]
      restartPolicy: Never

